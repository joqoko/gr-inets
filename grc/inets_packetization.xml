<?xml version="1.0"?>
<block>
  <name>packetization</name>
  <key>inets_packetization</key>
  <category>inets_dmf_mac</category>
  <import>import inets</import>
  <make>inets.packetization(develop_mode=$develop_mode, block_id=$block_id, constellation=$constellation, preamble=$preamble, samp_rate=$samp_rate, sps=$sps)</make>
  <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->
  <param>
    <name>Develop mode</name>
    <key>develop_mode</key>
    <type>enum</type>
    <hide>$develop_mode.hide</hide>
    <option>
      <name>off</name>
      <key>0</key>
      <opt>hide:part</opt>
      <opt>hide_control:all</opt>
    </option>
    <option>
      <name>on</name>
      <key>1</key>
      <opt>hide:none</opt>
      <opt>hide_control:none</opt>
    </option>
    <option>
      <name>time</name>
      <key>2</key>
      <opt>hide:none</opt>
      <opt>hide_control:none</opt>
    </option>
  </param>
  <param>
    <name>Block id</name>
    <key>block_id</key>
    <value>31</value>
    <type>int</type>
    <hide>$develop_mode.hide_control</hide>
  </param>

  <param>
    <name>constellation</name>
    <key>constellation</key>
    <value>gnuradio.digital.constellation_qpsk().base()</value>
    <type>raw</type>
    <hide>part</hide>
  </param>

  <param>
    <name>preamble</name>
    <key>preamble</key>
    <value>diff_preamble_128</value>
    <type>raw</type>
    <hide>part</hide>
  </param>

  <param>
    <name>Sample rate</name>
    <key>samp_rate</key>
    <value>samp_rate</value>
    <type>raw</type>
    <hide>part</hide>
  </param>

  <param>
    <name>sps</name>
    <key>sps</key>
    <value>sps</value>
    <type>raw</type>
    <hide>part</hide>
  </param>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>frame_in</name>
    <type>message</type>
    <optional>1</optional>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>packet_out</name>
    <type>message</type>
    <optional>1</optional>
  </source>
</block>
