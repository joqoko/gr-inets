<?xml version="1.0"?>
<block>
  <name>carrier_sensing_cpp_cc</name>
  <key>inets_carrier_sensing_cpp_cc</key>
  <category>inets_dmf_v1.0</category>
  <import>import inets</import>
  <make>inets.carrier_sensing_cpp_cc($cs_duration, $cs_threshold)</make>
  <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->
  <param>
    <name>Carrier sensing duration</name>
    <key>cs_duration</key>
    <type>float</type>
  </param>
  <param>
    <name>Carrier sensing threshold</name>
    <key>cs_threshold</key>
    <type>float</type>
  </param>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in</name>
    <type>complex</type>
  </sink>
  <sink>
    <name>spark_in</name>
    <type>message</type>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>out</name>
    <type>complex</type>
  </source>
  <source>
    <name>spark_out</name>
    <type>message</type>
  </source>
</block>
